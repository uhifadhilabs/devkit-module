<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true">
    <php>
        <env name="KERNEL_CLASS" value="Uhifadhi\Devkit\Tests\Integration\TestKernel"/>
        <!--
            Where the installation suite writes. Only the suite that boots the
            whole core needs it: the collector's own kernels own no entities and
            seed nothing of their own. Port 5434 is the local PostGIS cluster,
            5432 in CI.
        -->
        <env name="UHIFADHI_TEST_DATABASE_URL"
             value="postgresql://app:app@127.0.0.1:5434/uhifadhi_devkit_test?serverVersion=17&amp;charset=utf8"/>
    </php>
    <testsuites>
        <testsuite name="default">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source restrictNotices="true" restrictWarnings="true">
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
